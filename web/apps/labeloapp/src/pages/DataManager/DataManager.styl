.datamanager
  height calc(100vh - var(--header-height))

  // hack to change background of selected annotation in LSF inside DM
  #labelo-dm [class*=Annotations_annotation_selected]
    background: #e3f1ff
